# Nixpacks config for Sushi Fusion Frontend (Next.js)
# Coolify: set Base Directory to "frontend"

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
